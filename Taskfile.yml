version: '3'

dotenv:
  - '.env'
  - 'project.ini'

silent: true

vars:
  CLEANUP_DIRS:
    - .idea
    - .terraform
    - .vscode
  CLEANUP_FILES:
    - .DS_Store
    - .terraform.lock.hcl
    - terraform.tfstate
    - terraform.tfstate.backup
    - Desktop.ini
    - Thumbs.db
  DIST_DIR: dist
  MODULE: 'github.com/{{.GITHUB_USER}}/{{.GITHUB_REPO}}'
  PLATFORMS: >-
    darwin/amd64 darwin/arm64
    linux/386 linux/amd64 linux/arm64
    windows/386 windows/amd64 windows/arm64
  RELEASE_VERSION:
    sh: git describe --tags --abbrev=0 2>/dev/null || echo v0.0.0
  START_TIME: '{{now.UnixMilli}}'
  VERSION:
    sh: git describe --tags --always 2>/dev/null || echo dev
  VERSION_PKG: 'github.com/{{.GITHUB_USER}}/{{.GITHUB_REPO}}/internal/version'

tasks:
  default:
    cmds:
      - task --list

  _default:defer:
    internal: true
    cmds:
      - defer: &template-defer
          task: notify
          vars:
            NOTIFY_CONTEXT:
              map:
                START_TIME: '{{.START_TIME}}'
                NOTIFY_TITLE: '{{.TASK}} (go-task)'

  cleanup:
    aliases: [c]
    desc: 'Cleanup unnecessary files'
    cmds:
      - defer: *template-defer
      - task: cleanup:internal

  cleanup:internal:
    internal: true
    cmds:
      - task: cleanup:shell
      - task: cleanup:win

  cleanup:shell:
    internal: true
    dir: '{{.USER_WORKING_DIR}}'
    platforms: [darwin, linux]
    cmds:
      - for: { var: CLEANUP_DIRS }
        cmd: find . -type d -name "{{.ITEM}}" -print -prune -exec rm -rf {} + | sed 's/^/[DELETE]/'
      - for: { var: CLEANUP_FILES }
        cmd: find . -name "{{.ITEM}}" -print -delete | sed 's/^/[DELETE]/'

  cleanup:win:
    internal: true
    dir: '{{.USER_WORKING_DIR}}'
    platforms: [windows]
    cmds:
      - for: { var: CLEANUP_DIRS }
        cmd: powershell -NoProfile -Command $'Get-ChildItem -Path . -Recurse -Directory -Force -ErrorAction SilentlyContinue | Where-Object { $_.Name -eq "{{.ITEM}}" } | Sort-Object FullName -Descending | ForEach-Object { Write-Output ("[DELETE] $($_.FullName)"); Remove-Item -LiteralPath $_.FullName -Recurse -Force }'
      - for: { var: CLEANUP_FILES }
        cmd: powershell -NoProfile -Command $'Get-ChildItem -Path . -Recurse -File -Force -ErrorAction SilentlyContinue | Where-Object { $_.Name -eq "{{.ITEM}}" } | ForEach-Object { Write-Output ("[DELETE] $($_.FullName)"); Remove-Item -LiteralPath $_.FullName -Force }'

  go-build:
    aliases: [gb]
    desc: 'Build a binary for the current platform'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - task: go-clean:internal
      - echo "Building binary for {{OS}}/{{ARCH}}..."
      - go build -ldflags "-X {{.VERSION_PKG}}.Version={{.VERSION}}" -o {{.DIST_DIR}}/{{.BINARY_NAME}}{{if eq OS "windows"}}.exe{{end}} .

  go-build-all:
    aliases: [gba]
    desc: 'Cross-compile release binaries for all platforms'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - task: go-clean:internal
      - |
        set -e
        for target in {{.PLATFORMS}}; do
          goos="${target%/*}"
          goarch="${target#*/}"
          ext=""
          if [ "$goos" = "windows" ]; then ext=".exe"; fi
          out="{{.DIST_DIR}}/{{.BINARY_NAME}}_{{.RELEASE_VERSION}}_${goos}_${goarch}${ext}"
          echo "building ${out}"
          CGO_ENABLED=0 GOOS="$goos" GOARCH="$goarch" go build -ldflags "-X {{.VERSION_PKG}}.Version={{.RELEASE_VERSION}}" -o "$out" .
        done
      - task: go-hash:internal

  go-clean:
    aliases: [gc]
    desc: 'Delete the contents of the distribution directory'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - task: go-clean:internal

  go-clean:internal:
    dir: '{{.TASKFILE_DIR}}'
    internal: true
    cmds:
      - rm -rfv {{.DIST_DIR}}/*
      - mkdir -p {{.DIST_DIR}}

  go-hash:internal:
    dir: '{{.TASKFILE_DIR}}'
    internal: true
    platforms: [darwin, linux]
    vars:
      HASHTOOL:
        sh: |
          case "{{OS}}" in
            darwin) echo "shasum -a 256" ;;
            linux)  echo "sha256sum" ;;
          esac
    cmds:
      - |
        set -e
        : > "{{.DIST_DIR}}/checksums.txt"
        for binary in "{{.DIST_DIR}}"/*; do
          [ -f "$binary" ] || continue
          case "$binary" in
            */checksums.txt|*.sig) continue ;;
          esac
          name=${binary##*/}
          echo "hashing ${name}"
          (cd "{{.DIST_DIR}}" && {{.HASHTOOL}} "$name") >> "{{.DIST_DIR}}/checksums.txt"
        done

  go-mcp-inspector:
    aliases: [gm,gmcp]
    desc: 'Debugging with MCP Inspector'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - npx @modelcontextprotocol/inspector go run ./cmd/confgrep mcp -transport stdio

  go-run:
    aliases: [gr]
    desc: 'Run the application'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - go run . {{.CLI_ARGS}}

  go-run-debug:
    aliases: [grd]
    desc: 'Run the application (debug)'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - go run . -debug -config config_debug.yml {{.CLI_ARGS}}

  go-scan:
    aliases: [gs]
    desc: 'Scan the application'
    dir: '{{.TASKFILE_DIR}}'
    ignore_error: true
    cmds:
      - defer: *template-defer
      - |
        GREEN='\033[0;32m'
        RESET='\033[0m'
        printf '%b======================================== govulncheck%b\n' "$GREEN" "$RESET"
      - govulncheck ./...
      - |
        GREEN='\033[0;32m'
        RESET='\033[0m'
        printf '%b======================================== OSV-Scanner%b\n' "$GREEN" "$RESET"
      - osv-scanner -r .

  go-test:
    aliases: [gt]
    desc: 'Run the Go test suite'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - go vet ./...
      - go test ./...

  go-version:
    aliases: [gv]
    desc: 'Show version information'
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - defer: *template-defer
      - go run -ldflags "-X {{.VERSION_PKG}}.Version={{.VERSION}}" . -v

  notify:
    internal: true
    cmds:
      - task: notify:time
        vars:
          NOTIFY_CONTEXT:
            ref: .NOTIFY_CONTEXT
      - task: notify:toast
        vars:
          NOTIFY_CONTEXT:
            ref: .NOTIFY_CONTEXT

  notify:time:
    internal: true
    vars:
      END_TIME: '{{now.UnixMilli}}'
    cmds:
      - task: notify:time:shell
        vars:
          NOTIFY_CONTEXT:
            ref: .NOTIFY_CONTEXT
      - task: notify:time:win
        vars:
          NOTIFY_CONTEXT:
            ref: .NOTIFY_CONTEXT

  notify:time:shell:
    internal: true
    platforms: [darwin,linux]
    vars:
      END_TIME: '{{now.UnixMilli}}'
    cmds:
      - |
        ELAPSED_MS=$(({{.END_TIME}} - {{.NOTIFY_CONTEXT.START_TIME}}))
        MINUTES=$((ELAPSED_MS / 60000))
        SECONDS=$((ELAPSED_MS % 60000 / 1000))
        MILLIS=$((ELAPSED_MS % 1000))
        GREEN='\033[0;32m'
        RESET='\033[0m'
        if [ "$MINUTES" -gt 0 ]; then
          printf '%b======================================== Elapsed: %s min %s sec %s msec%b\n' "$GREEN" "$MINUTES" "$SECONDS" "$MILLIS" "$RESET"
        elif [ "$SECONDS" -gt 0 ]; then
          printf '%b======================================== Elapsed: %s sec %s msec%b\n' "$GREEN" "$SECONDS" "$MILLIS" "$RESET"
        else
          printf '%b======================================== Elapsed: %s msec%b\n' "$GREEN" "$MILLIS" "$RESET"
        fi

  notify:time:win:
    internal: true
    platforms: [windows]
    vars:
      END_TIME: '{{now.UnixMilli}}'
    cmds:
      - |
        powershell.exe -NoProfile -Sta -Command '$elapsedMs = [int64]{{.END_TIME}} - [int64]{{.NOTIFY_CONTEXT.START_TIME}}; $minutes = [math]::Floor($elapsedMs / 60000); $remainingMs = $elapsedMs - ($minutes * 60000); $seconds = [math]::Floor($remainingMs / 1000); $millis = $remainingMs - ($seconds * 1000); if ($minutes -gt 0) { $message = "======================================== Elapsed: {0} min {1} sec {2} msec" -f $minutes, $seconds, $millis } elseif ($seconds -gt 0) { $message = "======================================== Elapsed: {0} sec {1} msec" -f $seconds, $millis } else { $message = "======================================== Elapsed: {0} msec" -f $millis }; Write-Host $message -ForegroundColor Green'

  notify:toast:
    internal: true
    cmds:
      - task: notify:toast:mac
        vars:
          NOTIFY_CONTEXT:
            ref: .NOTIFY_CONTEXT
      - task: notify:toast:win
        vars:
          NOTIFY_CONTEXT:
            ref: .NOTIFY_CONTEXT

  notify:toast:mac:
    internal: true
    platforms: [darwin]
    vars:
      END_TIME: '{{now.UnixMilli}}'
    cmds:
      - osascript -e 'display notification "Done." with title "[Task] {{.NOTIFY_CONTEXT.NOTIFY_TITLE}}"'
      - /bin/sh -c 'nohup /usr/bin/afplay /System/Library/Sounds/Glass.aiff >/dev/null 2>&1 < /dev/null &'

  notify:toast:win:
    internal: true
    platforms: [windows]
    cmds:
      - powershell.exe -NoProfile -Sta -Command 'Add-Type -AssemblyName System.Windows.Forms; Add-Type -AssemblyName System.Drawing; [System.Media.SystemSounds]::Beep.Play(); $notify = New-Object System.Windows.Forms.NotifyIcon; $notify.Icon = [System.Drawing.SystemIcons]::Information; $notify.BalloonTipTitle = "[Task] {{.NOTIFY_CONTEXT.NOTIFY_TITLE}}"; $notify.BalloonTipText = "Done."; $notify.Visible = $true; $notify.ShowBalloonTip(5000); Start-Sleep -Seconds 5; $notify.Dispose()'
